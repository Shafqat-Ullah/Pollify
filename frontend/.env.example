# NOT required for production. The deployed frontend proxies /api/* to the
# Render backend via vercel.json, keeping requests same-origin (no CORS issues,
# refresh cookies work). Only the Vite dev proxy is used in local development.
VITE_API_URL=http://localhost:5000/api
